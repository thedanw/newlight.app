# UI/UX Audit — iOS HIG + high-level tokens (2026-08-17)

Source skill: `.agents/skills/ios-hig/`. Scope: styleguide shell, 4 custom components, theme/recipe token layer. Verdict: **strong — 2 token violations, 1 dead-knob regression, several a11y gaps.**

## Token discipline

Verified: `--radii-l1/l2/l3` chain compiles end-to-end (defs + `border-radius:var(--radii-l3)` usages in dist CSS; `RadiusToken` incl. `l1|l2|l3`; `data-radius` knob remaps). Colors: hex only in `colors/*.ts`; components use `colorPalette.*`/`fg.*`. ✅ elsewhere.

**NEEDS WORK**

- `recipes/nav-tile.ts:15` — `borderRadius:'lg'` → `'l3'` (raw scale ignores radius knob; tiles won't re-theme).
- `recipes/skeleton.ts:34` — `borderRadius:'9999px'` → `'full'` (bypasses tokens).
- `recipes/heading.ts` — **REGRESSION (dead knob)**: pristine CLI base `{fontWeight:'semibold'}` doesn't consume `var(--heading-font-weight|text-transform|color|letter-spacing)` (defined in `theme.css` §4). BrandForm "Heading style" checkboxes = silent no-ops. Lost in 2026-08-16 re-vendor (fixed B6, reintroduced). Fix: base reads the vars.

Intentional raw (keep): `'0'` joins, `'full'` circles, `'inherit'` switch, `var(--accordion-radius)`/`var(--avatar-radius)`, avatar `radii.full/l3` variants.

## iOS HIG

### Navigation
- `nav-tab-bar` — 🟡 Partial: 5-module sidebar = good tab-bar equiv, but NavTile `active` never passed (no current-section indicator).
- `nav-navigation-stack` — 🟡 Partial: push/pop + breadcrumb + back ✓; no URL/deep-link sync (module click = TODO).
- `nav-toolbar-placement` — ✅ (back lead, kebab trail, Apply/Cancel drawer footer).

### Interaction
- `inter-touch-targets` — 🔴 buttons <44px: default IconButton md=40px (header back, account, Dashboard search); BrandForm sm=36px. NavTile 90 / PullTab 54×48 ✓. (WCAG AA=24; HIG=44 → medium.)
- `inter-gesture-patterns` / `inter-keyboard-handling` — ✅
- `inter-list-search` — 🟡 filter hidden behind toggle; prefer visible search bar.
- drag-drop / pull-to-refresh / swipe — N/A.

### Accessibility
- `acc-labels` — 🔴 **PullTab has no accessible name** (`pull-tab.tsx`): hamburger aria-hidden, no aria-label. Add `aria-label={open?'Close navigation':'Open navigation'}` + `aria-expanded`.
- `acc-focus-management` — 🔴 Dashboard component "links" = `<Text onClick>` (not focusable); `Table.Root interactive` rows mouse-only (`table.tsx` no tabIndex/onKeyDown). Use real button/Link or role+tabIndex+onKeyDown.
- `acc-reduce-motion` — 🟡 App.tsx `useReducedMotion`→fade ✓, sidebar drag-tracking not gated (correct). `scrollIntoView({smooth})` ×2 in `Dashboard.tsx` ignores pref (minor).
- `acc-color-contrast` — 🟡 Verify `fg.subtle` (gray-10 ≈3.7:1 on light canvas) not used for small text (now placeholders/helpers only, fine). `fg.muted` ✓ AA.
- `acc-color-independent` / `acc-dynamic-type` (rem textStyles) / `acc-view-that-fits` (responsive grids) — ✅
- `acc-focus-management` (rings) — ✅ `focusVisibleRing` + NavTile/PullTab `_focusVisible`.

### User Feedback
- `feed-loading/error/empty/notifications` — ✅
- `feed-success-confirmation` — 🟡 no toast on BrandForm Apply; add `toaster.create`.

### UX Patterns
- `ux-modality` / `ux-data-entry` / `ux-settings` — ✅. `ux-undo`/`ux-confirmation-dialog` — N/A.

### Visual
- `vis-dark-mode` (semantic, no hex) / `vis-sf-symbols` (lucide) / `vis-layout-margins` — ✅

## Fix list (priority)
1. `heading.ts` wire `--heading-*` vars (dead knobs) — P1
1. `pull-tab.tsx` aria-label + aria-expanded — P1
2. `nav-tile.ts` `lg`→`l3`; `skeleton.ts` `9999px`→`full` — P2
2. Dashboard component links + table rows keyboard-accessible — P2
3. 44px touch targets (IconButtons/BrandForm buttons) — P3
3. `scrollIntoView` reduced-motion; toast on Apply; aria-pressed on Light/Dark — P3

Note: radius + heading edits need `pnpm panda` + Vite restart (stale generated CSS).
