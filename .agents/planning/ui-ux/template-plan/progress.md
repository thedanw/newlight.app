# Progress — UI/UX Design Lab Build

> Session log. Compaction target: end of each batch = ≤3 bullets here. Never carry raw tool output across batches (observation masking).

## Session log
- (init) Plan created 2026-08-09. Branch `feat/ui-design-lab` from `main`. Status: planning — no batches started.
- (B1) 2026-08-09 Scaffold done. Branch `feat/ui-design-lab` created; planning docs committed (`docs:`). Manual Vite react-ts scaffold (non-destructive — root had `.agents/`); `pnpm install` (react 19.2.8 / vite 7.3.6 / ts 5.8.3); `pnpm typecheck` clean (strict); `pnpm dev` boots, page serves at :5173. Note: scaffolded by hand because `create-vite` would prompt/overwrite on the non-empty root. Next: Batch 2 Panda CSS.
- (B2) 2026-08-09 Panda wired. `@pandacss/dev` 1.12.0 + postcss; `panda.config.ts` (`hash:false`, `outdir: styled-system`, `include src/**`, `jsxFramework react`); `postcss.config.mjs`; scripts `panda`/`prepare` (`panda codegen`); `src/index.css` holds the `@layer reset, base, tokens, recipes, utilities;` entry hook; `App.tsx` uses `css()` — dev renders (padding 8 / 2xl / bold verified via computed styles). Note: official Vite integration does NOT import `styled-system/styles.css`; the PostCSS plugin expands the `@layer` line in `src/index.css`. `styled-system/` gitignored (regenerated by `prepare`). Next: Batch 3 Park UI.
- (B3) 2026-08-09 Park UI vendored (no preset CLI model). Theme + palettes via `@park-ui/cli add __init sand orange red green` (NON-interactive — avoids `init` TTY): 11 theme files + colors into `src/core/theme/`, semantic tokens (fg/border/error, gray=sand, orange/red/green), recipes wired centrally via `src/core/theme/recipes/index.ts` imported by `panda.config.ts` (`theme.recipes`/`slotRecipes`) + `staticCss {recipes:{'*':'*'}}`. 25 base components + auto-pulled deps (close-button/icon/spinner/loader/span/group/absolute-center) vendored into `src/core/ui/`; barrel `src/core/ui/index.ts` = PascalCase named re-exports (#109). `Separator` (removed upstream) hand-added as own recipe + component. Aliases: `@/*`→`./src/*`, `styled-system/*`→`./styled-system/*` in tsconfigs; `vite-tsconfig-paths` in vite.config; `components.json` aliases → `src/core/{ui,theme,theme/recipes}`. Deps `@ark-ui/react` + `lucide-react` installed (CLI writes files only). `pnpm typecheck` clean. Next: Batch 4 theme+font.
- (B4) 2026-08-09 Theme emission + Inter done. `conditions.ts` gained `dark: '&[data-color-scheme=dark]'` → Panda now emits the `_dark` palette values under `[data-color-scheme=dark]` (verified: gray-1 light `#fdfdfc` / dark `#111110`) — before this the `_dark` tokens were dropped entirely (0 media blocks). Added missing `semanticTokens.radii` `l1=xs/l2=sm/l3=md` (Park UI real values, were absent → recipes' `radii.l2/l3` had no var). New un-layered `src/core/theme/theme.css` (imported after `index.css` in `main.tsx`) = the emission block: `[data-accent-color=orange|red|green]` re-maps all `--colors-color-palette-*` (scales 1-12/a1-12 + 20 semantic slots) so un-propped recipes take the accent; `[data-gray-color=sand]` placeholder; `[data-radius=none..2xl]` shifts `--radii-l1/l2/l3` (l2=knob, l1/l3 one step below/above); sidebar (`--sidebar-bg/fg/muted/subtle/border/accent/accent-fg`, light/dark/brand-dark/brand-light) + heading (`--heading-*` bold/uppercase/accent) shell vars; `color-scheme` + Inter binding + `--canvas-bg/fg`. `index.html` carries the defaults `data-color-scheme="light" data-accent-color="orange" data-gray-color="sand" data-radius="md" data-sidebar-style="light" data-heading-style=""` + Inter variable `<link>` (preconnect ×2). `App.tsx` = smoke harness (accent/radius/sidebar/scheme/heading knobs → `setAttribute` on `<html>`; live sample block). Verified in-browser: accent orange→red flips colorPalette solid-bg `#f76b15`→`#e5484d`; dark flips gray-1 `#fdfdfc`→`#111110` + `color-scheme:dark`; radius lg → l1/l2/l3 `0.5/0.75/1rem`; Inter applied. `pnpm typecheck` clean, `pnpm build` green. Next: Batch 5 SG shell + Dashboard.
- (B5) 2026-08-09 SG shell + Dashboard done. `src/styleguide/` built: `App.tsx` (waffle sidebar mock — People/Groups/Services/Calendar/Admin-Settings tiles → 'not-built' SlidePanel; `#page-panel` header with Breadcrumb trail + Back chevron; component-level push/pop panel stack via AnimatePresence + `durations.slowest` + `useReducedMotion`, no router), `toc.ts` (8 categories, 33 vendored components, `shipped` flag + `group` slot for B7), `pages/Dashboard.tsx` (TOC Cards + full table index via Tabs, toolPanel search/filter, featured Carousel strip, SlidePanel variant trio, loading/feedback strip), `pages/Category.tsx` (placeholder — B7 template), `Input.tsx` (local stand-in; B7 vendors the real one), `SlidePanel.tsx` (normal/fullscreen/immersive portal overlay, deterministic controlled mount/unmount — see errors #5/#6). `src/App.tsx` re-exports the shell; `framer-motion@13` installed; B4 smoke harness superseded. Verified: typecheck + build green; in-browser push/pop (breadcrumb Style Guide→Dashboard→Layout + Back), toolPanel "slider" → 1 row (Forms/Slider), all 3 SlidePanel variants open + close (X / Back chevron / backdrop). Next: Batch 6 BrandForm.
- (B6) 2026-08-09 Brand form done. `src/styleguide/BrandForm.tsx` (new) + header kebab in `App.tsx` (Menu + Avatar trigger → "Brand settings" opens SlidePanel 'normal'). 8 fields all wired to `<html>` data-* live re-theme (decision #16): logo Dropzone (png/svg/webp/jpg ≤1MB, 1 file, save-on-apply — pending logo previewed via object URL, revoked on cancel/unmount; committed URL owned by App, old revoked), Light/Dark segmented, Font/Gray/Accent/Sidebar-style Selects, radius native Slider + 7 Marks, heading-style checkboxes (space-separated attr). Logo commits to the sidebar brand slot on Apply. Verified in-browser (hidden-tab harness): kebab→Brand settings opens dialog; Dark → `data-color-scheme=dark`; accent Red → `data-accent-color=red` + colorPalette solid-bg `#f76b15`→`#e5484d`; radius → `data-radius=xl` + helper text; heading Bold/Uppercase/Accent → h2 weight 600→800, text-transform uppercase, color orange-11; Cancel closes, knobs stay live (by design). Fixed 2 real bugs (errors #7/#8). `pnpm typecheck` + `pnpm build` green. Next: Batch 7 subpages + catalog.

## Error table
| # | Date | Batch | Error | Fix / Workaround |
|---|---|---|---|---|
| 1 | 2026-08-09 | 2 | Vite 500: `Failed to resolve import \"./styled-system/styles.css\"` — file did exist at root but import is relative to `src/`; Panda Vite integration instead uses `src/index.css` with `@layer reset, base, tokens, recipes, utilities;` as the entry the PostCSS plugin expands. | Use the official pattern: `src/index.css` `@layer` line imported in `main.tsx`; never import `styled-system/styles.css` from `src`. `styled-system` sits at project root so from `src` use `../styled-system/*`. |
| 2 | 2026-08-09 | 3 | `@park-ui/cli add` aborted with generic "unexpected error" when given PascalCase ids (`Button`, `IconButton`, …) and `toaster`/`separator`. Registry ids are kebab-case (`icon-button`); `fetchRegistryItems` fetches `/react/{id}.json` (unknown id = 404 aborts whole batch); `Toaster`+`toaster` live inside the `toast` item; `Separator` no longer shipped upstream. | Use kebab-case ids; `add toast` for Toaster; hand-write `Separator` (recipe + `styled(ark.hr)` + barrel export). Vendored `file-upload.tsx` also carried Park UI's own `@/components/ui` import → rewrote to `./span`. |
| 3 | 2026-08-09 | 3 | magicast (CLI `updatePandaConfig`) wrote broken import specifiers `"@\\core\\theme\\..."` (doubled Windows backslashes) into `panda.config.ts` after `add __init …`. | Rewrote the 15 theme/color imports to forward-slash `@/core/theme/*`; `pnpm panda` codegen confirmed config resolves aliases fine. |
| 4 | 2026-08-09 | 4 | Semantic tokens' `_dark` values were NOT emitted at all — compiled CSS had light values only, 0 `@media`/`[data-color-scheme]` blocks → `data-color-scheme="dark"` was a no-op. Root cause: vendored `conditions.ts` overrides only `light` (`:root &, .light &`), so Panda's default `dark` should have been a media query, but with a selector-based `light` override the dark variants were dropped. | Added `dark: '&[data-color-scheme=dark]'` to `conditions.ts` → Panda emits `_dark` palette values under `[data-color-scheme=dark]` (attr selector beats the `:where(:root,.light)` base). Verified gray-1 light `#fdfdfc` / dark `#111110`. Also `semanticTokens.radii` (l1=xs/l2=sm/l3=md) was missing from the vendored theme → recipes' `radii.l2/l3` had no var; added per real `@park-ui/preset`. |
| 5 | 2026-08-09 | 5 | `durations.*` tokens missing from generated `styled-system/tokens/index.mjs` → `token('durations.slowest')` returned non-numeric → framer transition duration NaN/0s → all motion stuck at `initial` (opacity 0) and AnimatePresence exits never completed (panels never unmounted). Codegen output was stale (theme had `durations` registered but the generated module predated it). | Re-ran `pnpm panda` → `durations.*` now emitted; runtime `token('durations.slowest')` = `'400ms'`. |
| 6 | 2026-08-09 | 5 | `AnimatePresence` in a `createPortal` left exiting panels stuck in the DOM (exit animation reached its end state but elements were never removed). Initial diagnosis blamed the keyless-fragment wrapper; deeper root cause: the automation browser page is `visibilityState: hidden` → `requestAnimationFrame` never fires → framer-motion can't run or completion any animation (verified frames=0). | Rewrote `SlidePanel` to deterministic controlled mount/unmount (React `mounted`/`closing` state, state-driven `animate`, `setTimeout` unmount) — works in real AND headless browsers. Shell's push/pop stack keeps `AnimatePresence` (works where rAF is alive). |
| 7 | 2026-08-09 | 6 | BrandForm heading checkboxes were inert — toggling did nothing. `Checkbox.Root` was rendered with `Control`/`Indicator`/`Label` but NO `Checkbox.HiddenInput`; Ark Checkbox's `Control` is `aria-hidden` with no pointer handlers and the root `<label>` only forwards to the hidden native input (missing → no toggle path). Synthetic label clicks can't trigger implicit activation, but real-user clicks would also fail. | Added `<Checkbox.HiddenInput />` inside each `Checkbox.Root` (the vendored Park UI part is `HiddenInput`, not `Input`). Verified: `input.click()` (native activation) toggles → `data-heading-style` flips `bold` → `bold uppercase`. |
| 8 | 2026-08-09 | 6 | Heading-style knobs changed `data-heading-style` but had NO visual effect (h2 stayed weight 600 / no transform). Root cause: the `heading` recipe (`src/core/theme/recipes/heading.ts`) only set `fontWeight: 'semibold'` and never consumed the `--heading-*` shell vars theme.css emits (section 6). Vite also kept serving the pre-regen recipe CSS until the dev server was restarted. | Recipe now consumes `var(--heading-font-weight/-text-transform/-letter-spacing/-color)` with fallbacks matching the default look; aligned `:root` `--heading-font-weight` default to `var(--font-weights-semibold)` (was 700 — would've silently bolder every heading). Re-ran `pnpm panda` + restarted dev server. Verified: bold→800, uppercase→uppercase, accent→orange-11; default unchanged 600. |
