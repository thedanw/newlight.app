# Progress — UI/UX Design Lab Build

> Session log. Compaction target: end of each batch = ≤3 bullets here. Never carry raw tool output across batches (observation masking).

## Session log
- (init) Plan created 2026-08-09. Branch `feat/ui-design-lab` from `main`. Status: planning — no batches started.
- (B1) 2026-08-09 Scaffold done. Branch `feat/ui-design-lab` created; planning docs committed (`docs:`). Manual Vite react-ts scaffold (non-destructive — root had `.agents/`); `pnpm install` (react 19.2.8 / vite 7.3.6 / ts 5.8.3); `pnpm typecheck` clean (strict); `pnpm dev` boots, page serves at :5173. Note: scaffolded by hand because `create-vite` would prompt/overwrite on the non-empty root. Next: Batch 2 Panda CSS.
- (B2) 2026-08-09 Panda wired. `@pandacss/dev` 1.12.0 + postcss; `panda.config.ts` (`hash:false`, `outdir: styled-system`, `include src/**`, `jsxFramework react`); `postcss.config.mjs`; scripts `panda`/`prepare` (`panda codegen`); `src/index.css` holds the `@layer reset, base, tokens, recipes, utilities;` entry hook; `App.tsx` uses `css()` — dev renders (padding 8 / 2xl / bold verified via computed styles). Note: official Vite integration does NOT import `styled-system/styles.css`; the PostCSS plugin expands the `@layer` line in `src/index.css`. `styled-system/` gitignored (regenerated by `prepare`). Next: Batch 3 Park UI.
- (B3) 2026-08-09 Park UI vendored (no preset CLI model). Theme + palettes via `@park-ui/cli add __init sand orange red green` (NON-interactive — avoids `init` TTY): 11 theme files + colors into `src/core/theme/`, semantic tokens (fg/border/error, gray=sand, orange/red/green), recipes wired centrally via `src/core/theme/recipes/index.ts` imported by `panda.config.ts` (`theme.recipes`/`slotRecipes`) + `staticCss {recipes:{'*':'*'}}`. 25 base components + auto-pulled deps (close-button/icon/spinner/loader/span/group/absolute-center) vendored into `src/core/ui/`; barrel `src/core/ui/index.ts` = PascalCase named re-exports (#109). `Separator` (removed upstream) hand-added as own recipe + component. Aliases: `@/*`→`./src/*`, `styled-system/*`→`./styled-system/*` in tsconfigs; `vite-tsconfig-paths` in vite.config; `components.json` aliases → `src/core/{ui,theme,theme/recipes}`. Deps `@ark-ui/react` + `lucide-react` installed (CLI writes files only). `pnpm typecheck` clean. Next: Batch 4 theme+font.

## Error table
| # | Date | Batch | Error | Fix / Workaround |
|---|---|---|---|---|
| 1 | 2026-08-09 | 2 | Vite 500: `Failed to resolve import \"./styled-system/styles.css\"` — file did exist at root but import is relative to `src/`; Panda Vite integration instead uses `src/index.css` with `@layer reset, base, tokens, recipes, utilities;` as the entry the PostCSS plugin expands. | Use the official pattern: `src/index.css` `@layer` line imported in `main.tsx`; never import `styled-system/styles.css` from `src`. `styled-system` sits at project root so from `src` use `../styled-system/*`. |
| 2 | 2026-08-09 | 3 | `@park-ui/cli add` aborted with generic "unexpected error" when given PascalCase ids (`Button`, `IconButton`, …) and `toaster`/`separator`. Registry ids are kebab-case (`icon-button`); `fetchRegistryItems` fetches `/react/{id}.json` (unknown id = 404 aborts whole batch); `Toaster`+`toaster` live inside the `toast` item; `Separator` no longer shipped upstream. | Use kebab-case ids; `add toast` for Toaster; hand-write `Separator` (recipe + `styled(ark.hr)` + barrel export). Vendored `file-upload.tsx` also carried Park UI's own `@/components/ui` import → rewrote to `./span`. |
| 3 | 2026-08-09 | 3 | magicast (CLI `updatePandaConfig`) wrote broken import specifiers `"@\\core\\theme\\..."` (doubled Windows backslashes) into `panda.config.ts` after `add __init …`. | Rewrote the 15 theme/color imports to forward-slash `@/core/theme/*`; `pnpm panda` codegen confirmed config resolves aliases fine. |
