// Cross-app spacing contract — enforced by Panda responsive tokens
// Standard pair: { base: '3', md: '6' } (12px mobile / 24px desktop)
// Mirror in all recipes (page, card, sections, stacks) for consistent rhythm.

export const PAD = { base: '3', md: '6' } as const;
export const GAP = { base: '2', md: '4' } as const;
